apiVersion: v1
kind: Service
metadata:
  name: backend-api
spec:
  type: ClusterIP  # Internal only!
  selector:
    app: backend
    tier: api
  ports:
  - port: 8080
    targetPort: 5678
    protocol: TCP
